(function() {
    'use strict';

    angular
        .module('starter.profile.edit')
        .factory('profileEditService', profileEditService);

    profileEditService.$inject = [];

    /* @ngInject */
    function profileEditService() {
        var service = {
        }
        return service;
    }
})();
